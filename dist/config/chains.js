import { arbitrum, arbitrumSepolia, avalanche, base, baseSepolia, mainnet, plumeMainnet, sepolia, bsc, bscTestnet, } from 'viem/chains';
// TODO: convert to use the indexer to avoid hard coding
export const chains = [
    mainnet,
    sepolia,
    base,
    baseSepolia,
    arbitrum,
    arbitrumSepolia,
    avalanche,
    plumeMainnet,
    bsc,
    bscTestnet,
];
//# sourceMappingURL=chains.js.map